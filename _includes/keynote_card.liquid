<div class="keynote-card">
  <img src="{{ '/assets/img/' | append: keynote.image }}" alt="{{ keynote.name }}" class="keynote-photo">
  <h4>{{ keynote.name }}</h4>
  <p>{{ keynote.affiliation }}</p>
  <p><em>{{ keynote.position }}</em></p>
</div>
